'use strict';

describe('walletsApp.add module', function() {

  beforeEach(module('walletsApp.add'));

  describe('add controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var addCtrl = $controller('AddCtrl');
      expect(addCtrl).toBeDefined();
    }));

  });
});